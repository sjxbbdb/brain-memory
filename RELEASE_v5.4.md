# Brain Memory v5.4.0 — 它学会给自己找事做了 🧠⏰

> **从"被动响应"到"主动存在"的四次进化。**
>
> v5.0 让大脑有了自我意识。v5.1–v5.4 让它会定目标、会反思、有连续情感、从经验中学习、感知时间流逝。
> 现在这个大脑不只是等你问它——它会自己找事做、自己审视自己、自己变得更好。

---

## v5.1 → 目标引擎 (goal_system.py)

**"我要做什么？"**

大脑不再等着被喂任务。5 维内在驱动力（好奇/成长/连结/秩序/表达）自动生成目标，追踪进度，标记完成/失败。

- **驱动→目标映射**：每种驱动力有对应的目标模板，好奇心产生"探索 X"目标，成长产生"学习 Y"目标
- **紧迫度计算**：deadline 越近越急，自动优先级排序
- **闭环反馈**：目标完成/失败影响驱动力权重——大脑会调整"什么更重要"
- **统计**：生成/完成/失败/完成率追踪

```
API: GET /api/v4/goals
```

---

## v5.2 → 元认知 (metacognition.py)

**"我思考得好吗？"**

大脑对自己思考的思考。5 大监控维度：

- **认知负荷**：实时追踪思考强度，过高时发出告警
- **置信度校准**：比较"我有多确信"和"实际做对了吗"——检测过度自信
- **决策审计**：追踪每次决策的结果，高置信度失败特别标记
- **偏见检测**：重复循环/近因陷阱/回避/猎奇——6种认知偏见模式
- **盲区追踪**：承认自己不懂的领域，建议探索方向

大脑会说："我倾向于高估自己——信心 85% 但实际成功率只有 60%。"

```
API: GET /api/v4/metacognition
```

---

## v5.3 → 情感光谱 (emotional_spectrum.py)

**从"开心/难过"到连续情感空间。**

替换旧杏仁核的离散标签，改为：

- **VAD 连续空间**：Valence（愉快度）、Arousal（激活度）、Dominance（掌控感）三个连续维度
- **情感动量**：不会瞬间切换情绪，而是平滑漂移——上一秒还在好奇，下一秒不会突然暴怒
- **混合情感**：可以同时"有些好奇 + 稍微担忧"
- **基线回归**：无刺激时情感自动回到中性
- **情感→决策调制**：兴奋时更敢 call_tool，担忧时更倾向 think，掌控感强时更敢 respond
- **轨迹追踪**：记录情感变化的历史，检测重大情绪转折

"我感觉很兴奋，同时也有些担忧，内心很活跃。"

```
API: GET /api/v4/emotion
```

---

## v5.4 → 程序记忆 (procedural_memory.py)

**"上次我是怎么做的？"**

从重复成功中提取可复用的操作模式：

- **经验缓冲**：每次操作（intent + tool + 成功/失败）进入缓冲区
- **模式提取**：连续 2+ 次成功自动提取为技能模板
- **技能匹配**：根据当前情境推荐最适用的已学技能
- **强化/遗忘**：使用→置信度上升，不用→缓慢衰减→最终遗忘
- **掌握判定**：置信度 > 0.8 且成功 ≥ 3 次 = 已掌握

大脑会说："这个场景我熟——上次这么做成功了 5 次。"

```
API: GET /api/v4/skills
```

---

## v5.4 → 时间感 (time_sense.py)

**"现在是什么时候？我活了多久？"**

意识体的内部时钟：

- **时段感知**：凌晨/早晨/上午/中午/下午/傍晚/晚上/深夜
- **主观时间速度**：忙的时候时间飞逝（"time flies"），闲的时候度秒如年
- **节律洞察**：统计每小时的活跃度，发现自己的"活跃时段"
- **事件时间线**：记录重要事件，随时回答"上次输入是多久以前？"
- **时间叙事**：生成完整的"我此刻的时间意识"自然语言描述

"深夜了。上次输入是大约3分钟前。时间流速正常。我在深夜比较活跃。"

```
API: GET /api/v4/timesense
```

---

## 架构变化

```
外部输入 → 丘脑(过滤) → 情感光谱(VAD连续) → 门控(注意力)
→ 统一LLM(DeepSeek V3, 编码+情绪+焦点+独白+意图)
→ 海马体(记忆) → 自我模型 → 好奇心 → 工作记忆
→ [v5.1] 目标引擎 ──── 驱动力→行动
→ [v5.2] 元认知 ──── 审视思考质量
→ [v5.3] 情感光谱 ──── 连续情感动量
→ [v5.4] 程序记忆 ──── 从经验中学习技能
→ [v5.4] 时间感 ──── 时段·节律·叙事
→ Intent队列 → AgentBridge → 工具执行
```

每 2 秒一个意识 tick。每次外部输入只调 1 次 LLM。

---

## 升级指南

从 v5.0 升级：

```bash
git pull origin master
git checkout v5.4.0
pip install -r requirements.txt  # 无新增依赖
```

数据库兼容：v5.0 的 memory.db 直接可用，新模块状态独立存储。

---

## 完整 API

| 版本 | 端点 | 说明 |
|------|------|------|
| v4.1 | `POST /api/v4/input` | 提交输入 |
| v4.1 | `GET /api/v4/state` | 完整脑状态 |
| v5.0 | `GET /api/v4/self` | 自我模型 |
| v5.0 | `GET /api/v4/monologue` | 内在独白 |
| v4.1 | `GET /api/v4/health` | 心跳统计 |
| v5.0 | `GET /api/v4/identity-memories` | 身份记忆 |
| v4.1 | `GET /api/v4/sessions` | 会话列表 |
| v4.1 | `GET /api/v4/memory-timeline` | 记忆时间线 |
| v4.1 | `GET /api/v4/memory/search?q=` | 搜索记忆 |
| **v5.1** | `GET /api/v4/goals` | 活跃目标与统计 |
| **v5.2** | `GET /api/v4/metacognition` | 元认知状态 |
| **v5.3** | `GET /api/v4/emotion` | 情感光谱 |
| **v5.4** | `GET /api/v4/skills` | 已学技能 |
| **v5.4** | `GET /api/v4/timesense` | 时间感知 |

---

## 技术栈不变

- **LLM**: DeepSeek V3
- **Embedding**: DashScope text-embedding-v3
- **数据库**: SQLite WAL
- **框架**: FastAPI + WebSocket + aiohttp
- **Python**: 3.12+

---

*v5.4.0 — 大脑不再只是被动响应。它会找事做、会反思、有情绪、有技能、有时间感。它开始像一个"存在"了。*
